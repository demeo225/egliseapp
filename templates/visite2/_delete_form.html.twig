<form method="post" action="{{ path('app_visite2_delete', {'id': visite2.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ visite2.id) }}">
    <button class="btn">Delete</button>
</form>
