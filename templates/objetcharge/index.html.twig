{% extends "home/index.html.twig" %}

{% block title %}Objet depense{% endblock %}

{% block body %}



    <div class="card card-primary">
        <div class="card-header">
            <h3 class="card-title">Liste des objets de dépense </h3>
        </div>
 <div class="offset-1 col-md-10 mt-2 mb-2">
        {% for label, messages in app.flashes(['success', 'warning', 'danger']) %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} my-alert" role="alert">
                    <h1 class="m-0 flash-text">{{ message }}</h1>
                </div>
            {% endfor %}
        {% endfor %}
    </div>
        
        
        <!-- /.card-header -->
        <div class="car card-body">
            <a href='{{ path('app_objetcharge_new') }}'>
                <button class="btn btn-primary">Nouveau</button>
            </a>


            <hr style="visibility: hidden" />  
            <table id="example1" class="table table-bordered table-striped">

                <thead>
                    <tr>
                        <th>Libellé</th>
                        <th>Modifier</th>
                        <th>Supprimer</th>


                    </tr>
                </thead>
                <tbody>
                    {% for objetcharge in objetcharges %}
                        <tr>
                            <td>{{ objetcharge.libelle }}</td>
                     
                     



                            <td>
                                <a href="{{ path('app_objetcharge_edit', {'id': objetcharge.id}) }}">
                                    <span style="font-size: 1em; color: Dodgerblue;">
                                        <i class="fas fa-edit"></i>
                                    </span>
                                </a>
                            </td>
                            <td>
                                <form method="post" action="{{ path('app_objetcharge_delete', {'id': objetcharge.id}) }}" onsubmit="return confirm('Voulez-vous supprimer?');">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objetcharge.id) }}">
                                    <button class="btn">
                                        <span style="font-size: 1em; color: red;">
                                            <i class="fas fa-trash-alt"></i>
                                        </span>
                                    </button>
                                </form>
                            </td>
                        </tr>
                
                   
                    {% endfor %}
                </tbody>
            </table>
        </div>

    </div>




{% endblock %}


