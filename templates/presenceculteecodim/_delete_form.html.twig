<form method="post" action="{{ path('app_presenceculteecodim_delete', {'id': presenceculteecodim.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ presenceculteecodim.id) }}">
    <button class="btn">Delete</button>
</form>
