{% extends "home/index.html.twig" %}

{% block title %}Patrimoine
{% endblock %}

{% block body %}

	<h3>
		<u>
			<strong>Details Ethnies</strong>
		</u>
	</h3>

	<table class="table">
            
		<thead>
			<tr>
				<th>Date</th>
				<th>Articles</th>
				<th>Quantité</th>
				<th>Prix unitaire</th>
				<th>Prix total</th>
				<th>Enregistré le</th>
				<th>Enregistré par</th>
				
				<th>Action</th>
			</tr>
		</thead>
		<tbody>
			<tr>
				
                            <td>{{ patrimoine.datepatrimoine|date('d-m-Y')}}</td>
                            <td>{{ patrimoine.article}}</td>
                            <td>{{ patrimoine.quantite}}</td>
                            <td>{{ patrimoine.prixunitaire}}</td>
                            <td>{{ patrimoine.prixtotal}}</td>
                            <td>{{ patrimoine.createAt|date('d-m-Y')}}</td>
                            <td>{{ patrimoine.createdBy.nomuser}} {{patrimoine.createdBy.prenom|capitalize}}</td>
                         
				<td>
					<a href="{{ path('patrimoine_update', {'id': patrimoine.id}) }}">
						<span style="font-size: 2em; color: Dodgerblue;">

							<i class="fas fa-edit"></i>

						</span>
					</a>


				</td>
			</tr>
		</tbody>
	</td>
</tr></tbody><tfooter>
<tr>
	<td>
		<a href='{{ path('patrimoine') }}'>
			<button class="btn btn-primary">Retour à la liste</button>
		</a>
	</td>
	<td>
		<form method="post" action="{{ path('patrimoine_delete', {'id': patrimoine.id}) }}" onsubmit="return confirm('etes vous sur de vouloir supprimer?');">
			<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ patrimoine.id) }}">
			<button class="btn btn-danger">
				Delete
			</button>
		</form>

	</td>

</tr></tfooter></form></table></i>{% endblock %}
