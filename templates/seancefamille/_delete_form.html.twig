<form method="post" action="{{ path('seancefamille_delete', {'id': seancefamille.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seancefamille.id) }}">
    <button class="btn">Delete</button>
</form>
