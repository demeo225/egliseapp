{% extends 'home/index.html.twig' %}
{% block title %}{{ eglise.denomination }} : Cotisations de: {{ nomgpe|upper }} {% endblock %}

{% block body %}



    <section class="content">
        <div class="container-fluid">
            <div class="row">

                <!-- /.card-header -->
                <div class="card-body">

                    <a href='{{ path('groupe') }}'>
                        <button class="btn btn-success">Retour à la liste</button>
                    </a>
                        <div class="btn btn-primary">
                            Cotisations de : {{ nomgpe|upper }} </div>
                    <hr style="visibility: hidden" />
                    <table id="example1" class="table table-bordered table-striped">
                        <thead>
                            <tr>
                                <th>Groupe</th>
                                <th>Objet</th>
                                <th>Montant</th>
                                <th>Date buttoire</th>
                                <th>Etat</th>

                            </tr>
                        </thead>
                        <tbody>
                            {% for cotisationgroupe in cotisationgroupes %}
                                <tr>

                                    <td> {% if cotisationgroupe.groupe is not null  %} {{ cotisationgroupe.groupe.nom|upper }}{% else %} {% endif %}</td>
                                    <td>{{ cotisationgroupe.objet }}</td>
                                    <td>{{ cotisationgroupe.montant }}</td>
                                    <td>{{ cotisationgroupe.datefin ? cotisationgroupe.datefin|date('d-m-Y') : '' }}</td>

                                    <td>
                                        {% if date('now') < date(cotisationgroupe.datefin) %}
                                            <b>En cours</b>

                                        {% else %}

                                            <i class="btn btn-success">  Achévée</i>
                                        {% endif %}
                                    </td>


                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="16">Aucune donnée trouvée</td>
                                </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>

            </div>


        </div>
    </section>


{% endblock %}
