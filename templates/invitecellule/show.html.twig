{% extends 'home/index.html.twig' %}

{% block title %}Invitecellule{% endblock %}

{% block body %}
    <h1>Invitecellule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>CreateAt</th>
                <td>{{ invitecellule.createAt ? invitecellule.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ invitecellule.updateAt ? invitecellule.updateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ invitecellule.nom }}</td>
            </tr>
            <tr>
                <th>Contact</th>
                <td>{{ invitecellule.contact }}</td>
            </tr>
            <tr>
                <th>Habitation</th>
                <td>{{ invitecellule.habitation }}</td>
            </tr>
            <tr>
                <th>Sexe</th>
                <td>{{ invitecellule.sexe }}</td>
            </tr>
            <tr>
                <th>Fonction</th>
                <td>{{ invitecellule.fonction }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ invitecellule.id }}</td>
            </tr>
            <tr>
                <th>CreatedFromIp</th>
                <td>{{ invitecellule.createdFromIp }}</td>
            </tr>
            <tr>
                <th>UpdatedFromIp</th>
                <td>{{ invitecellule.updatedFromIp }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ invitecellule.deletedAt ? invitecellule.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedFromIp</th>
                <td>{{ invitecellule.deletedFromIp }}</td>
            </tr>
            <tr>
                <th>Editable</th>
                <td>{{ invitecellule.editable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_invitecellule_index') }}">back to list</a>

    <a href="{{ path('app_invitecellule_edit', {'id': invitecellule.id}) }}">edit</a>

    {{ include('invitecellule/_delete_form.html.twig') }}
{% endblock %}
