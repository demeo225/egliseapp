<form method="post" action="{{ path('cotisationgroupe_delete', {'id': cotisationgroupe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cotisationgroupe.id) }}">
    <button class="btn">Delete</button>
</form>
