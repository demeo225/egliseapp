<form method="post" action="{{ path('dimeglobale_delete', {'id': dimeglobale.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer cet enregistrement');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ dimeglobale.id) }}">
    <button class="btn">Supprimer</button>
</form>
