<form method="post" action="{{ path('cotisationcellule_delete', {'id': cotisationcellule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cotisationcellule.id) }}">
    <button class="btn">Delete</button>
</form>
