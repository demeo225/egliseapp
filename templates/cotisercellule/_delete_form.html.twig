<form method="post" action="{{ path('cotisercellule_delete', {'id': cotisercellule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cotisercellule.id) }}">
    <button class="btn">Delete</button>
</form>
