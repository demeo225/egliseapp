{% extends "home/index.html.twig" %}

{% block title %}Cotisation {% endblock %}

{% block body %}
    <h1>Cotisatisercellule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cotisercellule.id }}</td>
            </tr>
            <tr>
                <th>Montantpayer</th>
                <td>{{ cotisercellule.montantpayer }}</td>
            </tr>
            <tr>
                <th>Reste</th>
                <td>{{ cotisercellule.reste }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('cotisercellule_index') }}">back to list</a>

    <a href="{{ path('cotisercellule_edit', {'id': cotisercellule.id}) }}">edit</a>

    {{ include('cotisercellule/_delete_form.html.twig') }}
{% endblock %}
