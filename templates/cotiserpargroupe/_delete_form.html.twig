<form method="post" action="{{ path('app_cotiserpargroupe_delete', {'id': cotiserpargroupe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cotiserpargroupe.id) }}">
    <button class="btn">Delete</button>
</form>
