<form method="post" action="{{ path('app_objetrecette_delete', {'id': objetrecette.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ objetrecette.id) }}">
    <button class="btn">Delete</button>
</form>
