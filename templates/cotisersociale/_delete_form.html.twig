<form method="post" action="{{ path('app_cotisersociale_delete', {'id': cotisersociale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cotisersociale.id) }}">
    <button class="btn">Delete</button>
</form>
