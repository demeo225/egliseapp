<form method="post" action="{{ path('app_activitesociale_delete', {'id': activitesociale.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ activitesociale.id) }}">
    <button class="btn">Delete</button>
</form>
