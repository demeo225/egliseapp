<form method="post" action="{{ path('app_invitefamille_delete', {'id': invitefamille.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invitefamille.id) }}">
    <button class="btn">Delete</button>
</form>
