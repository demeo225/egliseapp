<form method="post" action="{{ path('app_nominer_delete', {'id': nominer.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ nominer.id) }}">
    <button class="btn">Delete</button>
</form>
