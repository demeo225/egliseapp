<form method="post" action="{{ path('cotiserdepartement_delete', {'id': cotiserdepartement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cotiserdepartement.id) }}">
    <button class="btn">Delete</button>
</form>
