<form method="post" action="{{ path('app_cotisationparcellule_delete', {'id': cotisationparcellule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cotisationparcellule.id) }}">
    <button class="btn">Delete</button>
</form>
