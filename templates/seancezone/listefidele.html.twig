


<div class="card card-primary">

    <div class="card-header">
        <h3 class="card-title">Liste des invités  </h3>
    </div>

    <!-- /.card-header -->
    <div class="card-body">

        <table id="example1" class="table table-bordered table-striped">

            <thead>
                <tr>
                    <th>Nom & prenoms</th>
                    <th>Genre</th>
                    <th>Teléphone</th>
                    <th>Lieu de residence</th>
                    <th>Fonction</th>



                </tr>
            </thead>
            <tbody>
                {% for invitezone in invitezones %}
                    <tr>
                        <td>{{ invitezone.nom|upper}}</td>
                        <td>{{ invitezone.sexe}}</td>
                        <td>{{ invitezone.contact}}</td>
                        <td>{{ invitezone.habitation}}</td>
                        <td>{{ invitezone.fonction}}</td>

                    </tr>
                         
                {% endfor %}
            </tbody>
        </table>

    </div>

</div>


</div>

