{% extends 'home/index.html.twig' %}

{% block title %}Invitedepartement index{% endblock %}

{% block body %}
    <div class="card card-primary">
        <div class="card-header">
            <h3 class="card-title">Liste des invités de departement</h3>
        </div>
 <div class="offset-1 col-md-10 mt-2 mb-2">
        {% for label, messages in app.flashes(['success', 'warning', 'danger']) %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} my-alert" role="alert">
                    <h1 class="m-0 flash-text">{{ message }}</h1>
                </div>
            {% endfor %}
        {% endfor %}
    </div>
        <div class="card-body">
            <a href="{{ path('app_invitedepartement_new') }}">
                <button class="btn btn-primary">Nouveau</button></a>
            <a href={{ path('seancedepartement_index') }}>
                <button class="btn btn-success">Liste activités</button>
            </a>


            <hr style="visibility: hidden" />
            <table id="example1" class="table table-bordered table-striped">
                <thead>
                    <tr>

                        <th>Date</th>
                        <th>Departement</th>
                        <th>Nom</th>
                        <th>Contact</th>
                        <th>Habitation</th>
                        <th>Sexe</th>
                        <th>Fonction</th>

                        <th>Modifier</th>
                        <th>Suppimer</th>
                    </tr>
                </thead>
                <tbody>
                    {% for invitedepartement in invitedepartements %}
                        {% if is_granted("invitedepartement_edit", invitedepartement) %}
                            <tr>
                                <td> {% if invitedepartement.seancedepartement is not empty %} {{ invitedepartement.seancedepartement.datesuper|date('d-m-Y') }} {% else %} {% endif %}</td>
                                <td> {% if invitedepartement.seancedepartement is not empty %} {{ invitedepartement.seancedepartement.departement.nom }} {% else %} {% endif %}</td>

                                <td>{{ invitedepartement.nom }}</td>
                                <td>{{ invitedepartement.contact }}</td>
                                <td>{{ invitedepartement.habitation }}</td>
                                <td>{{ invitedepartement.sexe }}</td>
                                <td>{{ invitedepartement.fonction }}</td>

                                <td>
                                         <button type="button"  id="btn-fideles-modal" data-target="{{ path('app_invitedepartement_edit', {'id': invitedepartement.id}) }}"> 
                                        <span style="font-size: 1em; color: Dodgerblue;">
                                            <i class="fas fa-edit" title="Modifier"></i>
                                        </span>
                                    </button>
                                </td>
                                 <td>
                                <form method="post" action="{{ path('app_invitedepartement_delete', {'id': invitedepartement.id}) }}" onsubmit="return confirm('Voulez-vous supprimer?');">
                                    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invitedepartement.id) }}">
                                    <button class="btn">
                                        <span style="font-size: 1em; color: red;">
                                            <i class="fas fa-trash-alt"></i>
                                        </span>
                                    </button>
                                </form>
                            </td>
                            </tr>
                        {% endif %}
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>


    <!-- Modal des fidèles -->
    <div class="modal fade bd-example-modal-lg" id="fidelesModal" tabindex="-1" aria-labelledby="fidelesModalLabel" aria-hidden="true">

        <div class="modal-dialog modal-lg">
            <div class="modal-content">
                <div class="modal-body">

                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-success" data-dismiss="modal">Fermer</button>
                </div>
            </div>
        </div>
    </div>
{% endblock %}

{% block javascripts %}
    {{ parent() }}
    <script>
        $(document).ready(function () {
            // quand on clic sur le button voir les fideles pour ouvrir la fenêtre modal
            $(document).on('click', 'button#btn-fideles-modal', function () {
                // créer le modal qui porte l'id fidelesModal
                let modal = new bootstrap.Modal(document.getElementById('fidelesModal'), {
                    keyboard: false
                });

                // recupere le div class="modal-body" du div id="fidelesModal"
                let container = $('#fidelesModal .modal-body');

                $.ajax({
                    type: 'POST',
                    url: $(this).attr('data-target'),
                    async: true,
                    beforeSend: function () { // avant d'envoyé le post on vide le div.modal-body
                        container.empty();
                    },
                    success: function (html) {
                        container.append(html); // une fois la réponse, on rempli le div.modal-body
                        modal.show(); // on affiche la fenêtre modal
                        //console.log(html);
                    }
                });
            });
        });
    </script>



{% endblock %}