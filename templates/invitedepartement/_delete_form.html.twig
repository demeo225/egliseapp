<form method="post" action="{{ path('app_invitedepartement_delete', {'id': invitedepartement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invitedepartement.id) }}">
    <button class="btn">Delete</button>
</form>
