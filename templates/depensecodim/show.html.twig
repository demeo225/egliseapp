{% extends 'base.html.twig' %}

{% block title %}Depensecodim{% endblock %}

{% block body %}
    <h1>Depensecodim</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ depensecodim.id }}</td>
            </tr>
            <tr>
                <th>Datedepense</th>
                <td>{{ depensecodim.datedepense ? depensecodim.datedepense|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Objet</th>
                <td>{{ depensecodim.objet }}</td>
            </tr>
            <tr>
                <th>Montant</th>
                <td>{{ depensecodim.montant }}</td>
            </tr>
            <tr>
                <th>Detail</th>
                <td>{{ depensecodim.detail }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ depensecodim.createAt ? depensecodim.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ depensecodim.updateAt ? depensecodim.updateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedFromIp</th>
                <td>{{ depensecodim.createdFromIp }}</td>
            </tr>
            <tr>
                <th>UpdatedFromIp</th>
                <td>{{ depensecodim.updatedFromIp }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ depensecodim.deletedAt ? depensecodim.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedFromIp</th>
                <td>{{ depensecodim.deletedFromIp }}</td>
            </tr>
            <tr>
                <th>Editable</th>
                <td>{{ depensecodim.editable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_depensecodim_index') }}">back to list</a>

    <a href="{{ path('app_depensecodim_edit', {'id': depensecodim.id}) }}">edit</a>

    {{ include('depensecodim/_delete_form.html.twig') }}
{% endblock %}
