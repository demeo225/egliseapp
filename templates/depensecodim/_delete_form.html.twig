<form method="post" action="{{ path('app_depensecodim_delete', {'id': depensecodim.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depensecodim.id) }}">
    <button class="btn">Delete</button>
</form>
