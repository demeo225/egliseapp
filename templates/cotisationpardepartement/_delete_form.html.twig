<form method="post" action="{{ path('app_cotisationpardepartement_delete', {'id': cotisationpardepartement.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cotisationpardepartement.id) }}">
    <button class="btn">Delete</button>
</form>
