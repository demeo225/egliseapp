{% extends 'home/index.html.twig' %}


{% block title %}
    Details {{ nomcotisationpardepartement|upper }} {% endblock %}

    {% block body %}



        <div class="card card-primary">

            <div class="card-header">
                <h3 class="card-title">Fidèles ayant cotisé pour: {{ nomcotisationpardepartement|upper }} </h3>
            </div>
            <h3><b> </b></h3>
            <div class="container-fluid">
                <div class="row">

                    <!-- /.card-header -->
                    <div class="card-body">

                        <a href='{{ path('app_cotisationpardepartement_index') }}'>
                            <button class="btn btn-success">Liste des cotisations</button>
                        </a>
                        {% for cotiserpardepartement in cotiserpardepartements %}
                            
                            {% set sum = cotiserpardepartement.montantpayer + sum ?? 0 %}
                            {% if loop.last  %} Total cotisé :<b> {{ sum|number_format(0, '.', ' ')}}F CFA -- {{ sum|format_number(style='spellout')}} F CFA</b>  {% endif %}
                             
                        {% endfor %}
                        <hr style="visibility: hidden" />
                        <table id="example1" class="table table-bordered table-striped">
                            <thead>
                                <tr>
                                    <th>Objet</th>
                                    <th>Departement</th>
                                    <th>Montant </th>
                                    <th>Montant payé</th>
                                    <th>Reste</th>
                                  
                                </tr>
                            </thead>
                            <tbody>
                                {% for cotiserpardepartement in cotiserpardepartements %}
                                        <tr>
                                            <td> {% if cotiserpardepartement.cotisationpardepartement is not null  %} {{ cotiserpardepartement.cotisationpardepartement.objet|upper }}{% else %} {% endif %}</td>
                                            <td>{% if cotiserpardepartement.departement is not null  %}{{ cotiserpardepartement.departement.nom|upper }}{% else %} {% endif %}</td>
                                            <td>{{ cotiserpardepartement.cotisationpardepartement.montant|number_format(0, '.', ' ') }}</td>
                                            <td>{{ cotiserpardepartement.montantpayer|number_format(0, '.', ' ') }}</td>
                                            <td>  {% if  cotiserpardepartement.reste == 0 %} <b>Soldé</b> 
                                            {% elseif cotiserpardepartement.reste >0 %} {{ cotiserpardepartement.reste|number_format(0, '.', ' ') }} {% elseif cotiserpardepartement.reste < 0 %} 
                                                <i class="btn btn-success"> Soldé </i> {% endif %}

                                            </td>
                                   
                                    {% else %}
                                        <tr>
                                            <td colspan="4">Aucune donnée trouvée</td>
                                        </tr>
                                        {% endfor %}
                                        </tbody>
                                    </table>
                                </div>

                            </div>


                        </div>
                    </div>


                    {% endblock %}
