<form method="post" action="{{ path('couple_delete', {'id': couple.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ couple.id) }}">
    <button class="btn">Delete</button>
</form>
