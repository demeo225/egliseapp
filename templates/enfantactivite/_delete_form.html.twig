<form method="post" action="{{ path('enfantactivite_delete', {'id': enfantactivite.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ enfantactivite.id) }}">
    <button class="btn">Delete</button>
</form>
