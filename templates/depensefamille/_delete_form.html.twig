<form method="post" action="{{ path('app_depensefamille_delete', {'id': depensefamille.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depensefamille.id) }}">
    <button class="btn">Delete</button>
</form>
