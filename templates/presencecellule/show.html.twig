{% extends "home/index.html.twig" %}

{% block title %}Presencecellule{% endblock %}

{% block body %}
    <h1>Presencecellule</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ presencecellule.id }}</td>
            </tr>
            <tr>
                <th>CreatedFromIp</th>
                <td>{{ presencecellule.createdFromIp }}</td>
            </tr>
            <tr>
                <th>UpdatedFromIp</th>
                <td>{{ presencecellule.updatedFromIp }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ presencecellule.deletedAt ? presencecellule.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedFromIp</th>
                <td>{{ presencecellule.deletedFromIp }}</td>
            </tr>
            <tr>
                <th>Editable</th>
                <td>{{ presencecellule.editable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_presencecellule_index') }}">back to list</a>

    <a href="{{ path('app_presencecellule_edit', {'id': presencecellule.id}) }}">edit</a>

    {{ include('presencecellule/_delete_form.html.twig') }}
{% endblock %}
