<form method="post" action="{{ path('app_presencecellule_delete', {'id': presencecellule.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ presencecellule.id) }}">
    <button class="btn">Delete</button>
</form>
