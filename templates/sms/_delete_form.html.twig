<form method="post" action="{{ path('sms_delete', {'id': sms.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sms.id) }}">
    <button class="btn">Delete</button>
</form>
