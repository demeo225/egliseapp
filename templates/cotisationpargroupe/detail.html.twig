{% extends 'home/index.html.twig' %}


{% block title %}
    Details {{ nomcotisationpargroupe|upper }} {% endblock %}

    {% block body %}



        <div class="card card-primary">

            <div class="card-header">
                <h3 class="card-title">Fidèles ayant cotisé pour: {{ nomcotisationpargroupe|upper }} </h3>
            </div>
            <h3><b> </b></h3>
            <div class="container-fluid">
                <div class="row">

                    <!-- /.card-header -->
                    <div class="card-body">

                        <a href='{{ path('app_cotisationpargroupe_index') }}'>
                            <button class="btn btn-success">Liste des cotisations</button>
                        </a>
                        {% for cotiserpargroupe in cotiserpargroupes %}
                            
                            {% set sum = cotiserpargroupe.montantpayer + sum ?? 0 %}
                            {% if loop.last  %}  Total cotisé :<b> {{ sum|number_format(0, '.', ' ')}}F CFA -- {{ sum|format_number(style='spellout')}} F CFA</b>  {% endif %}
                            
                        {% endfor %}
                        <hr style="visibility: hidden" />
                        <table id="example1" class="table table-bordered table-striped">
                            <thead>
                                <tr>
                                    <th>Objet</th>
                                    <th>Groupe</th>
                                    <th>Montant </th>
                                    <th>Montant payé</th>
                                    <th>Reste</th>
                                  
                                </tr>
                            </thead>
                            <tbody>
                                {% for cotiserpargroupe in cotiserpargroupes %}
                                        <tr>
                                            <td> {% if cotiserpargroupe.cotisationpargroupe is not null  %} {{ cotiserpargroupe.cotisationpargroupe.objet|upper }}{% else %} {% endif %}</td>
                                            <td>{% if cotiserpargroupe.groupe is not null  %}{{ cotiserpargroupe.groupe.nom|upper }}{% else %} {% endif %}</td>
                                            <td>{{ cotiserpargroupe.cotisationpargroupe.montant|number_format(0, '.', ' ') }}</td>
                                            <td>{{ cotiserpargroupe.montantpayer|number_format(0, '.', ' ') }}</td>
                                            <td>  {% if  cotiserpargroupe.reste == 0 %} <b>Soldé</b> 
                                            {% elseif cotiserpargroupe.reste >0 %} {{ cotiserpargroupe.reste|number_format(0, '.', ' ') }} {% elseif cotiserpargroupe.reste < 0 %} 
                                                <i class="btn btn-success"> Soldé </i> {% endif %}

                                            </td>
                                   
                                    {% else %}
                                        <tr>
                                            <td colspan="4">Aucune donnée trouvée</td>
                                        </tr>
                                        {% endfor %}
                                        </tbody>
                                    </table>
                                </div>

                            </div>


                        </div>
                    </div>


                    {% endblock %}
