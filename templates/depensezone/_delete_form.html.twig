<form method="post" action="{{ path('app_depensezone_delete', {'id': depensezone.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depensezone.id) }}">
    <button class="btn">Delete</button>
</form>
