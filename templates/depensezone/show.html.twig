{% extends "home/index.html.twig" %}

{% block title %}Depensezone{% endblock %}

{% block body %}
    <div class="card card-primary">
        <div class="card-header">
            <h3 class="card-title">Details depenses du {{ depensezone.datedepense ? depensezone.datedepense|date('d-m-Y') : '' }}</h3>
        </div>

        <table class="table">
            <tbody>

                <tr>
                    <th>Date depense</th>
                    <td>{{ depensezone.datedepense ? depensezone.datedepense|date('d-m-Y') : '' }}</td>
                </tr>
                <tr>
                    <th>Objet</th>
                    <td>{{ depensezone.objet }}</td>
                </tr>
                <tr>
                    <th>Montant</th>
                    <td>{{ depensezone.montant|number_format(0, '.', ' ')  }}</td>
                </tr>

                <tr>
                    <th>Detail</th>
                    <td>{{ depensezone.detail }}</td>
                </tr>


            </tbody>
        </table>
        <div  class="col-md-2"><a href="{{ path('app_depensezone_index') }}" class="btn btn-success">Afficher la liste</a> </div>
        <form method="post" action="{{ path('app_depensezone_delete', {'id': depensezone.id}) }}" onsubmit="return confirm('Voulez-vous supprimer?');">
            <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ depensezone.id) }}">
            <button class="btn">
                <span style="font-size: 1em; color: red;">
                    <i class="fas fa-trash-alt" title="Supprimer"></i>
                </span>
            </button>
        </form> 
    </div>
{% endblock %}
