{% extends "home/index.html.twig" %}

{% block title %}Cotiserzone{% endblock %}

{% block body %}
    <h1>Cotiserzone</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cotiserzone.id }}</td>
            </tr>
            <tr>
                <th>Montantpayer</th>
                <td>{{ cotiserzone.montantpayer }}</td>
            </tr>
            <tr>
                <th>Reste</th>
                <td>{{ cotiserzone.reste }}</td>
            </tr>
            <tr>
                <th>CreateAt</th>
                <td>{{ cotiserzone.createAt ? cotiserzone.createAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>UpdateAt</th>
                <td>{{ cotiserzone.updateAt ? cotiserzone.updateAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>CreatedFromIp</th>
                <td>{{ cotiserzone.createdFromIp }}</td>
            </tr>
            <tr>
                <th>UpdatedFromIp</th>
                <td>{{ cotiserzone.updatedFromIp }}</td>
            </tr>
            <tr>
                <th>DeletedAt</th>
                <td>{{ cotiserzone.deletedAt ? cotiserzone.deletedAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>DeletedFromIp</th>
                <td>{{ cotiserzone.deletedFromIp }}</td>
            </tr>
            <tr>
                <th>Editable</th>
                <td>{{ cotiserzone.editable ? 'Yes' : 'No' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_cotiserzone_index') }}">back to list</a>

    <a href="{{ path('app_cotiserzone_edit', {'id': cotiserzone.id}) }}">edit</a>

    {{ include('cotiserzone/_delete_form.html.twig') }}
{% endblock %}
