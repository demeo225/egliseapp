<form method="post" action="{{ path('app_invitegroupe_delete', {'id': invitegroupe.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ invitegroupe.id) }}">
    <button class="btn">Delete</button>
</form>
