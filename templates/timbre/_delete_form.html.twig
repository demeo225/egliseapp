<form method="post" action="{{ path('app_timbre_delete', {'id': timbre.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ timbre.id) }}">
    <button class="btn">Delete</button>
</form>
