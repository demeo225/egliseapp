<form method="post" action="{{ path('app_cotiserparfamille_delete', {'id': cotiserparfamille.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cotiserparfamille.id) }}">
    <button class="btn">Delete</button>
</form>
